// Legacy page from the link-based verification flow — replaced by the
// 4-digit OTP in Settings → Account. Safe to delete this folder.
import { Component } from '@angular/core';

@Component({
  selector: 'app-verify-email',
  template: '',
})
export class VerifyEmail {}
