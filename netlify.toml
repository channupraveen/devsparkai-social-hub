[build]
  command = "npm run build"
  publish = "dist/devsparkai-social-hub/browser"

# SPA fallback — Angular router handles all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
